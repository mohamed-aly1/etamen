import 'package:flutter/material.dart';

class TopNurses extends StatelessWidget {
  const TopNurses({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      appBar: AppBar(),
      body: Center(child: Text('TopNurses')),
    );
  }
}
